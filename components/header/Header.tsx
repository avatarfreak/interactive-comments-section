import React from "react";

interface Props {}

const Header = () => {
  return (
    <header>
      <h1 className="sr-only">Interactive Comments Section</h1>
    </header>
  );
};

export default Header;
